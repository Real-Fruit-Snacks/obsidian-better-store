import { Plugin } from "obsidian";

export default class BetterStorePlugin extends Plugin {
  async onload(): Promise<void> {
    // View, settings, and data service are registered in later tasks.
  }
}
