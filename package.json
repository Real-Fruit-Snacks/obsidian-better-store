{
  "name": "better-store",
  "version": "0.1.1",
  "description": "A polished community plugin browser for Obsidian",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production && node scripts/verify-bundle.mjs",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "esbuild-svelte": "^0.9.0",
    "obsidian": "latest",
    "svelte": "^5.0.0",
    "svelte-preprocess": "^6.0.0",
    "tslib": "^2.6.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
